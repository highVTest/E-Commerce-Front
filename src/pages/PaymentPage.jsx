import React from 'react';
import { Center, Title } from '@mantine/core';

const PaymentPage = () => {
    return (
        <Center style={{ height: '100vh', textAlign: 'center' }}>
            <Text order={1} style={{ fontSize: '48px', color: 'black' }}>결제 페이지</Text>
        </Center>
    );
};

export default PaymentPage;
